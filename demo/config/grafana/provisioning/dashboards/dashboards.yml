apiVersion: 1

providers:
  - name: PowerAuth Dashboards
    orgId: 1
    folder: PowerAuth
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
